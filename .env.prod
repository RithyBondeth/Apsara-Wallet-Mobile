APP_NAME=Apsara Wallet
# TODO(deploy): replace with the production API domain once the Railway
# production service has one. See apsara-wallet-api/DEPLOYMENT.md.
# Must be https and must keep the /api/v1 suffix.
API_BASE_URL=https://apsara-wallet-api-production.up.railway.app/api/v1
DEBUG_MODE=false

# TODO(deploy): paste the Sentry project DSN (sentry.io → Project → Settings →
# Client Keys). Leaving it blank ships with crash reporting disabled — the app
# still runs, you just find out about crashes from your users instead.
SENTRY_DSN=
